# Feature Status — Source of Truth

Legend: `Not Started` / `In Progress` / `Implemented (untested)` / `Done (tested)`

Priorities: P0 = MVP, P1 = Important, P2 = Advanced, P3 = Experimental

Last updated: 2026-08-19 (Phase 25)

## Free Mode

| Feature | Priority | Status | Notes |
|---|---|---|---|
| Repository discovery | P0 | Done (tested) | Phase 2 — languages (Java/JS/TS), build system (Maven/npm/Gradle-detected), package managers (npm/pnpm/yarn), frameworks (heuristic, evidence-based), Git branch/working-tree status. Path-sandboxed. 21 backend tests. |
| Repository indexing (.gitignore aware) | P0 | Done (tested) | Phase 3 — per-file language/LOC/size/hash/test-classification, regex-based import extraction (JS/TS/Java), nested `.gitignore` merging. Symbol-level extraction (Tree-sitter) not implemented — see gap note below. |
| Repository dashboard | P0 | Done (tested) | Phase 4 — repo name/path, languages+LOC, build system, package managers, frameworks, Git branch/working-tree status, total/test file counts, last-scan timestamp. Real API wiring, not mock data. |
| Architecture explorer | P0 | Done (tested) | Phase 5 — files aggregated into directory-depth modules (configurable depth 1-3), JS/TS relative-import and Java FQN-import resolution, module-level dependency edges with weights, external-dependency summary. Table-based UI, not a raw node graph (avoids the "unreadable thousands-of-nodes" trap). |
| Deterministic static analysis engine | P0 | Done (tested) | Phase 6 — rule-based pipeline (`large-file`, `large-function`, `todo-fixme-density`, `missing-test-file`, `hardcoded-secret`), evidence never fabricated, secrets always redacted in evidence. `missing-test-file` uses BFS transitive closure over the import graph (reused from Architecture explorer) plus naming-convention fallback — an earlier direct-import-only version produced 16-18 false positives when dogfooded against this project's own `backend/src`; fixed and reverified down to 2 genuine findings, 0 false positives. |
| Findings system | P0 | Done (tested) | Phase 7 — `POST /projects/:id/analysis` runs the engine and persists findings (transactional replace, tracked via `analysis_run`); `GET /projects/:id/findings` lists with severity/category filters. Findings UI page with severity-colored badges, filters, and a "Run Analysis" button. |
| Git analysis | P0 | Done (tested) | Phase 8 — `GET /projects/:id/git`: branch/working-tree status (reused from Phase 2 detection), recent commit log (hash/author/date/message, ASCII unit-separator parsing so a "\|"-containing commit subject can't corrupt fields), file churn (commit count per file within a configurable day window), uncommitted-changes diff stat (staged+unstaged vs HEAD, `git diff HEAD --numstat`). Computed live off `.git` each call, not persisted (state changes on every commit; a cached view would mislead). Surfaced as a "Git activity" section on the Dashboard page rather than a new nav item — Git Analysis was never one of the product's 9 top-level sections, and this keeps repo-health signals in one place. All `git` invocations via `execFileSync`, never shell-interpolated. |
| Test runner (Maven/npm/Vitest) | P0 | Done (tested) | Phase 9 — `POST /projects/:id/tests/run` detects a real runnable command (Maven `mvn test`; npm/pnpm/yarn `test` script, labeled `vitest` when that's the declared framework or generic `npm-script` otherwise) and executes it via `child_process.spawn` with an argv array (never a shell string), in its own process group so a timeout kills the whole tree, not just the immediate `npm`/`mvn` process. Vitest and Maven Surefire summary lines are parsed into pass/fail/skip counts; an unrecognized framework's counts are left `null` (unknown) rather than fabricated. Persisted to `test_run` (present but unused since Phase 0; extended via migration 003 with `status`/`reason` columns). `GET /projects/:id/tests` (history) and `GET /projects/:id/tests/:runId` (single run incl. full stdout/stderr). Tests page: run button, latest-run summary, expandable output, run history. Gradle is reported as "not yet supported" rather than attempted — matches the existing "Gradle-detected-only" scope note from Phase 2. Two real dogfooding bugs caught: (1) the Vitest summary regex was anchored on a line starting with "Tests", but Vitest's colored reporter wraps that label in an ANSI escape code, so the line never matched — fixed by stripping ANSI codes before parsing, verified by rerunning the parser against this project's own real `npm test` output (both backend, 77/77, and frontend, 25/25, parsed correctly); (2) the first execution approach used `execFile`'s built-in `timeout`, which only kills the immediate child — since `npm test` spawns a grandchild process for the actual test binary, a timeout would leave that grandchild running orphaned; fixed by spawning detached and killing the whole process group. |
| Dependency analysis | P0 | Done (tested) | Phase 10 — `GET /projects/:id/dependencies`, computed live off the manifest/lockfile each call (same pattern as Architecture/Git). npm/pnpm/yarn: direct deps from `package.json`; duplicate-version detection parses `package-lock.json`'s v2/v3 flat `packages` map — v1 (nested-tree) lockfiles and missing lockfiles get an honest "not supported"/"no lockfile" note rather than fabricated results. Maven: direct deps via regex `<dependency>` block extraction from `pom.xml` (doesn't distinguish `<dependencyManagement>`, unresolved `${property}` placeholders reported as literal text); duplicate-version detection explicitly not attempted for Maven (would require resolving the full tree via `mvn dependency:tree`, which this product doesn't invoke) — honest note returned instead. Dashboard "Dependencies" section shows direct-dependency count and any duplicate-version groups. Dogfooded against this project's own real `backend/` and `frontend/` `package-lock.json` files — correctly found real duplicate-version groups in both. |
| Security analysis (deterministic) | P0 | Done (tested) | Phase 10 — `GET /projects/:id/security`, computed live (not persisted) by running only the security-category subset of the analysis engine (`SECURITY_RULES`) fresh off disk each call — a live/current view distinct from the historical `GET /findings?category=security`. Three new rules alongside the existing `hardcoded-secret`: `env-file-committed` (flags real `.env`-style files, excluding `.env.example`/`.sample`/`.template`/`.defaults`/`.dist` templates; never echoes file contents), `permissive-cors` (flags `Access-Control-Allow-Origin: *` and `cors({ origin: true / '*' })`-shaped patterns), `disabled-tls-verification` (flags `rejectUnauthorized: false`, `NODE_TLS_REJECT_UNAUTHORIZED=0`, Python `verify=False`, and an empty Java `checkServerTrusted()`). Dogfooding caught a real self-referential false-positive bug: the new rules' own source files (label strings and an explanatory comment) contained the exact vulnerable syntax they were built to detect, so the rules flagged themselves — fixed by paraphrasing all labels/comments to describe the pattern rather than literally quote it (two rounds — the first fix's own explanatory comment also had to be rewritten after it re-introduced a literal quoted match). Reverified clean (0 findings) against this project's own real `backend/` and `frontend/`, and confirmed all 4 rules still correctly fire against synthetic vulnerable fixtures. |
| Audit report | P0 | Done (tested) | Phase 11 — `GET /projects/:id/audit` aggregates this product's existing data sources into one consolidated view: it runs no new analysis of its own, only combines what's already there. Persisted data reused as-is: the latest discovery/index snapshot, the latest `POST /analysis` run's finding counts (by severity/category), the latest test run. Computed fresh on every call, same as their standalone endpoints: the live security scan, live dependency analysis, live Git activity — deliberately not cached, since a stale audit report would be worse than a slightly slower one. Every "never happened yet" case (unscanned, unanalyzed, untested) is reported honestly (`null`/empty, not fabricated). `GET /projects/:id/audit/export` renders the same aggregate as a standalone Markdown document (`Content-Disposition: attachment`) for the "Download report (.md)" button — evidence in the exported security-finding sections is already-redacted (redaction happens at the rule layer, not re-checked here). Audit page replaces the Phase 0 placeholder; first frontend surface for the live security-scan endpoint (added in Phase 10 but never given its own page). Dogfooded via a live server against this project's own real `backend/`, both before (empty states) and after running discover+index+analysis on it (real snapshot/findings/dependency-duplicate data). |
| Additional language support (beyond Java/JS/TS) | P2 | Not Started | Post-MVP |
| Symbol-level extraction (Tree-sitter: classes/functions/exports) | P1 | Not Started | Deferred from Phase 3 — current `imports` extraction is regex-based, not AST-based |
| Visual dependency graph (node/edge diagram) | P2 | Not Started | Phase 5 shipped a table-based aggregated view, not a rendered graph diagram — deliberate scope choice, table is filterable/readable at any repo size without a graphing library |

## AI Mode

| Feature | Priority | Status | Notes |
|---|---|---|---|
| AI provider abstraction | P0 | Done (tested) | Phase 12 — `AIProvider` interface (`listModels`, `complete`, `estimateTokens`, `checkStatus`) per `docs/AI_MODE.md` §2; business logic depends only on this interface, never a vendor SDK. One real adapter implemented: `openai-compatible` (`backend/src/ai/provider/adapters/openaiCompatible.ts`), talking real HTTP to any server implementing the OpenAI Chat Completions API shape (covers OpenAI itself plus local/self-hosted servers like Ollama's or LM Studio's OpenAI-compatible endpoints) — real `fetch`-based GET `/models` and POST `/chat/completions`, with HTTP 401/403 → `auth_error`, 429 → `rate_limited`, network failure/timeout → `unreachable`. `anthropic-compatible` and `ollama` (native API) are named in the target design but have no adapter yet — `registry.ts` throws an honest "not yet supported" error for them rather than faking one. `provider_configuration` (DB table, empty since Phase 0) now has its first real writer: `GET/POST/PATCH/DELETE /api/v1/ai/providers`, plus `POST .../check-status` and `GET .../models` for live connectivity checks. The raw API key (migration 004 added the column) is stored server-side only and never appears in any API response — only a masked preview (`sk-1...cd` style, matching the Findings redaction convention) and a `hasApiKey` boolean are returned. AI Mode page (replacing the Phase 0 placeholder): add/list/enable/disable/delete a provider, "Check status" button (only fires on explicit click, never automatically). Explicitly scaffolding only — nothing calls `complete()` from a real user-facing feature yet; that starts at Phase 14 once Phase 13's context engine exists. Dogfooded end-to-end via a live server against a real local HTTP double speaking the OpenAI-compatible protocol (create → list, confirming the API key never appears in the response body → check-status → list models → a real `complete()` call), and the new migration was verified to upgrade an existing pre-Phase-12 database (only migrations 001-003 applied, real data already in `provider_configuration`) without data loss. |
| AI context selection engine | P0 | Done (tested) | Phase 13 — `selectContextForFinding()` (`backend/src/ai/context/select.ts`) builds a `ContextBundle` for a `Finding` target per `docs/AI_MODE.md` §3's selection order: directly affected file → directly relevant methods/functions → imported symbols → known callers → associated test file(s) → relevant config → relevant Git diff hunk. "Directly relevant methods/functions" is approximated by windowing the primary file around the finding's line range (200/80/30/10-line windows, largest that fits first) when the full file doesn't fit the budget — this product has no AST parser (same documented regex-based-imports tradeoff as Architecture/Phase 3), so true function-boundary extraction isn't attempted rather than faked. Imported/caller files reuse the same `resolveImports()` import graph as the Architecture explorer and the `missing-test-file` rule. Test-file discovery prefers a test file that actually imports the primary file over a naming-convention guess. Relevant config is the same root-level `package.json`/`pom.xml` manifest Phase 10's dependency analyzer already reads. The Git diff hunk uses a new `getUncommittedDiffForFile()` (`backend/src/git/fileDiff.ts`, argv-safe `execFileSync`, same pattern as `commits.ts`/`churn.ts`/`diffStat.ts`) and is only offered when the file actually has uncommitted changes. Every candidate's content is redacted via a new shared module, `backend/src/security/secretPatterns.ts` (the `hardcoded-secret` rule's redaction logic factored out so the exact same patterns and redaction format protect AI context as protect analysis evidence), before it is ever counted toward the token budget or returned — per `docs/SECURITY.md` §4. Token counting reuses Phase 12's chars/4 estimator (factored into `backend/src/ai/tokenEstimate.ts` so the adapter and the selector can't drift). Every included and excluded item carries an honest, human-readable reason, with real computed token costs in exclusion reasons — nothing fabricated. `GET /api/v1/projects/:id/findings/:findingId/context?budgetTokens=N` (default 4000) exposes it. Deliberately scoped to `Finding` targets only — `TestRun`/refactor-request targets are named in `docs/AI_MODE.md` §3 but have no real downstream consumer yet (that starts at Phase 14), so they are explicitly not implemented rather than stubbed. Minimal frontend surface: a "Preview AI context" toggle on each finding in the Findings page, showing included/excluded items with reasons and token counts — nothing consumes the bundle yet (no AI feature exists that actually sends it anywhere); this is a preview only. Dogfooded against this project's own real `backend/` source: at a 4000-token budget, correctly selected the primary file plus two genuinely-imported files (totaling 3999/4000 tokens) while honestly excluding five other imports, a known caller, and a test-file caller with accurate token-cost reasons; at an artificially tight 100-token budget, correctly excluded even the smallest line-window of the primary file, citing the real full-file token count. One genuine bug caught during test-writing (not just a test bug): a caller file that was also a test file was being added to the candidate list twice (once via the generic "known caller" loop, once via the test-file-specific loop), so the more specific "Test file that imports X" reason was silently shadowed by the generic one — fixed by excluding test-file callers from the generic loop before it runs. |
| AI finding explanation | P0 | Done (tested) | Phase 14 — the first phase to actually call `AIProvider.complete()` from a real user-facing workflow, using Phase 12's provider and Phase 13's context bundle together for the first time. `explainFinding()` (`backend/src/ai/workflows/explainFinding.ts`) builds a Phase 13 `ContextBundle` (now with an `includeContent: true` option added for exactly this — the Phase 13 preview API still defaults to the lightweight summary, so the existing preview payload doesn't grow), assembles a system+user prompt (finding summary + the bundle's actual, already-redacted file contents), calls the configured provider, and persists an accounting record. `provider_configuration.finding_id`/`ai_response.content` (migration 005, extending the Phase 0 `ai_request`/`ai_response` scaffold tables) let a previously-generated explanation be shown again without re-spending tokens. Strictly read-only: never writes to the finding, never applies anything — root-cause analysis, fix planning, and patch generation (Phases 15-17) are separate, later workflows. API: `POST /api/v1/projects/:id/findings/:findingId/explain` (the only route in the product that spends real tokens against a real provider — only runs on an explicit call, never automatically, per docs/AI_MODE.md §1) and `GET .../explanation` (read-only lookup of the latest successful explanation, no provider call). Uses whichever enabled provider is passed by ID, or the first enabled one if none is specified; returns an honest 400 if no provider is configured and enabled, and classifies provider failures (401→auth_error, 429→rate_limited, other→unreachable) into the matching HTTP status. Findings page: an "AI explanation" toggle per finding, showing a stored explanation if one exists or a "Generate explanation" button — visibly disabled (not hidden), with an inline explanation, when no AI provider is enabled, per docs/AI_MODE.md §1's Free/AI Mode boundary rule. Dogfooded end-to-end against this project's own real `backend/` source and a real local HTTP double speaking the OpenAI-compatible protocol: registered the project, ran discover+index+analysis for real, configured and enabled a provider, called `/explain` on a genuine `large-function` finding in `src/ai/context/select.ts` — confirmed the actual prompt sent to the provider contained the real finding summary and real (redacted) file content, confirmed the response and token usage were returned and persisted, and confirmed `GET .../explanation` served the identical stored text back without a second provider call. Migration 005 was also verified to upgrade a simulated pre-existing database (only migrations 001-004 applied, real `ai_request`/`ai_response` rows already present) without data loss. |
| AI root-cause analysis | P0 | Done (tested) | Phase 15 — `analyzeRootCause()` (`backend/src/ai/workflows/rootCauseAnalysis.ts`), sharing the exact same context-selection/provider-call/accounting plumbing as Phase 14's `explainFinding` via a new `runFindingWorkflow()` helper (factored out of `explainFinding.ts` once a second workflow needed the identical bookkeeping — zero-regression refactor, full suite reverified green). Unlike Phase 14's plain-prose explanation, this asks the provider to respond in a fixed `EVIDENCE:`/`INFERENCE:`/`CONFIDENCE:` format so the response can separate what the shown code directly demonstrates from what the model believes beyond that, per docs/AI_MODE.md §4's "Root Cause Analysis" workflow step. `parseRootCauseSections()` parses that format leniently and honestly: any section the model's response doesn't clearly contain is left `null` rather than guessed, while the full raw response is always preserved so a parsing miss never loses information, just structure — a real bug here (the EVIDENCE regex not stopping at a missing INFERENCE: header, so it swallowed the CONFIDENCE: line into the evidence bullets) was caught by test-writing and fixed. Reuses the same `ai_request`/`ai_response` accounting tables as Phase 14 (distinguished by `operation_type`), so no new migration was needed — verified the two operation types stay independent for the same finding. API: `POST /api/v1/projects/:id/findings/:findingId/root-cause` (explicit call only, same 400/401/429/502 handling as `/explain`, via a new shared `resolveEnabledProvider()` route helper) and `GET .../root-cause` (read-only, re-parses the stored raw response on every fetch rather than persisting the parsed shape, so a future parser improvement benefits old rows automatically). Findings page: a "Root-cause analysis" toggle showing evidence as a bulleted list and inference as prose, kept visually distinct, with a collapsible raw-response fallback when parsing didn't fully succeed — same visibly-disabled-when-no-provider pattern as Phase 14. Dogfooded end-to-end against this project's own real `backend/` source and a real local HTTP double: called on a genuine `large-function` finding in `src/ai/context/select.ts`, confirmed the real EVIDENCE/INFERENCE/CONFIDENCE-formatted prompt and response, confirmed `GET .../root-cause` served the same parsed result back, and confirmed a subsequent `/explain` call on the same finding didn't disturb the stored root-cause analysis (and vice versa). |
| AI fix planning | P0 | Done (tested) | Phase 16 — `planFix()` (`backend/src/ai/workflows/fixPlan.ts`) produces the exact seven-section plan docs/AI_MODE.md §5 requires (Problem, Root cause, Files affected, Proposed changes, Risks, Required tests, Validation strategy). First workflow to actively consume a *previous* workflow's output: if a successful Phase 15 root-cause analysis already exists for the finding, its raw evidence/inference/confidence text is folded into the prompt as grounding (`usedPriorRootCauseAnalysis` reports whether this happened); otherwise the plan is built from the finding and context bundle alone, never blocking on or fabricating a root-cause analysis that doesn't exist. Both the 3-section root-cause parser and this 7-section fix-plan parser now share one generalized parser, `parseStructuredSections()` (`backend/src/ai/workflows/parseStructuredResponse.ts`) — extracted once a second differently-sized structured format needed the same "split into named sections honestly" logic Phase 15 built bespoke; the generalization (each section terminates at *any* later header, not just the next one) is a strict superset of the fix Phase 15 shipped for its own EVIDENCE/CONFIDENCE bug, and `rootCauseAnalysis.ts` was refactored onto it with the full suite reverified green before Phase 16's own tests were added. Any of the seven sections the model's response doesn't clearly contain is left `null` — never guessed — with the complete raw response always preserved. Strictly advisory: produces prose describing a proposed change, never a diff, and changes nothing on disk — patch generation (Phase 17) is a separate, later workflow gated behind the two human-approval checkpoints in docs/AI_MODE.md §4's workflow diagram. API: `POST /api/v1/projects/:id/findings/:findingId/fix-plan` (same 400/401/429/502 handling as `/explain` and `/root-cause`, via the shared `resolveEnabledProvider()` route helper) and `GET .../fix-plan` (read-only, re-parses the stored raw response on every fetch). Findings page: a "Fix plan" toggle rendering all seven sections plus files-affected as a bulleted list, with a raw-response fallback and the same visibly-disabled-when-no-provider pattern as Phases 14-15. Dogfooded end-to-end against this project's own real `backend/` source and a real local HTTP double: ran a real root-cause analysis on a genuine `large-function` finding in `src/ai/context/select.ts`, then a real fix-plan call on the same finding — confirmed the second call's prompt actually contained the first call's stored root-cause text (`usedPriorRootCauseAnalysis: true`), confirmed all seven sections parsed correctly from the response, and confirmed `GET .../fix-plan` served the identical result back. |
| AI patch generation (diff, human-approved) | P0 | Done (tested) | Phase 17 — the first AI-Mode phase whose output could eventually touch disk, so unlike Phases 14-16's single request/response calls, it's a real persisted state machine (`patch`/`patch_review`, Phase 0 scaffold tables, empty/unused until now): `pending_approval -> approved -> proposed` (has a diff), or `pending_approval -> rejected`. Both gates docs/AI_MODE.md §4 requires are enforced server-side against the real `patch.status` column, not just hidden/shown in the UI: creating a patch never calls a provider (it only registers intent, requiring a Phase 16 fix plan to already exist — 400 "Generate a fix plan for this finding first." otherwise); `/generate` is refused with a 400 unless `status === "approved"`, verified by a test (and the dogfood run) that calls `/generate` before `/approve` and confirms it's rejected. `generatePatch()` (`backend/src/ai/workflows/generatePatch.ts`) reuses `runFindingWorkflow()` unchanged from Phases 15-16, folds the finding's latest successful Phase 16 fix plan into the prompt as grounding when one exists (`usedFixPlan` reports whether this happened, mirroring Phase 16's `usedPriorRootCauseAnalysis` pattern), and asks the provider for ONLY a unified diff — or `NO_PATCH: <reason>` if it can't produce one, preserved verbatim rather than treated as an error. Deliberately scoped: the returned diff is stored raw, exactly as returned — never parsed, never validated (no `git apply --check` dry-run), and never written to any file; the route layer only ever updates the `patch` row's `diff_text`/`status` columns. Applying an approved, reviewed diff to disk is out of scope until Phase 18's second human-approval gate. Migration 006 relaxes `patch.diff_text` from `NOT NULL` to nullable (SQLite table-rebuild pattern, since there's no `ALTER COLUMN`) so a `pending_approval` patch can exist with no diff yet. Migration-safety verification (the same simulate-a-real-pre-existing-database methodology used for every schema change since Phase 12) caught a real bug: `DROP TABLE patch` while `foreign_keys=ON` cascade-deleted real `patch_review` rows via `ON DELETE CASCADE` before the rename completed, losing data mid-migration. Fixed at the shared migration-runner level (`backend/src/db/index.ts`), not just this one migration — `runMigrations()` now toggles `PRAGMA foreign_keys` OFF before running the whole batch of pending migrations and back ON in a `finally` afterward (it can't be toggled inside a transaction, so this had to wrap the batch, not any single migration), benefiting every future rebuild-style migration. Reverified with an expanded safety script confirming data survival AND that FK enforcement is correctly restored afterward. API: `POST/GET /api/v1/projects/:id/findings/:findingId/patches` (create/list), `GET /api/v1/projects/:id/patches/:patchId` (single), `POST .../approve` and `POST .../reject` (the human-approval gate, recording a `patch_review` row), `POST .../generate` (the only route that spends tokens, gated on `status === "approved"`). Findings page: a "Patches" toggle showing a "Create patch (requires a fix plan)" button, then per-patch Approve/Reject buttons while `pending_approval`, a "Generate diff" button (disabled with an explanatory message when no provider is enabled, same pattern as Phases 14-16) once `approved`, a "Rejected before generation." message if rejected, and the raw diff rendered in a `<pre>` block once generated. Redaction scope note (confirmed by a test-authoring correction, not a product bug): secret redaction applies to context sent TO the provider, not to the provider's own returned diff — a generated diff can legitimately quote a secret being removed (a `-const apiKey = "..."` line), exactly as a real diff would. 235 backend tests (patchRepo: 4, generatePatch workflow: 3, routes: 5 new incl. the full create->approve->generate lifecycle and the reject-then-refuse-generate path), 62 frontend tests (21 in Findings.test.tsx covering create/approve/reject/generate and the disabled-when-no-provider case). Dogfooded end-to-end against this project's own real `backend/` directory (not a synthetic fixture): registered the real project, ran a real discover+index+analysis (11 real findings), generated a real fix plan for a genuine `large-function` finding, then walked create -> premature-generate-refused(400) -> approve -> generate against a real local HTTP double, confirming `usedFixPlan: true` and the diff persisted and re-fetchable. |
| Diff review & approval workflow | P0 | Done (tested) | Phase 18 — the second and final human-approval gate docs/AI_MODE.md §4's workflow diagram requires ("Diff Review → Human Approval → Apply Patch"), and the first phase in the entire product to actually write to a file on disk. Extends the `patch` state machine: `proposed` (Phase 17's end state, a real diff) `-> approved_for_apply -> applied`, or `proposed -> rejected` (a second, separate rejection distinct from Phase 17's pre-generation reject) — `applied`/`failed` match docs/ARCHITECTURE.md §3's originally-specified status enum. `POST .../approve-apply` and `POST .../reject-apply` mirror Phase 17's first-gate routes exactly, recording a `patch_review` row (`approved_for_apply`/`rejected_after_review`) and requiring `status === "proposed"` server-side. `POST .../apply` is the only route in the product that writes to a file — gated on `status === "approved_for_apply"` (or `"failed"`, allowing a mechanical retry without re-litigating the human decision that the change itself is worth applying) — and calls a new `applyPatchToDisk()` (`backend/src/patch/applyPatch.ts`) that always runs a real `git apply --check` dry run first and only performs the real write if that succeeds; a dry-run failure (e.g. the file drifted since the diff was generated) never reaches the write step. Uses `execFileSync("git", [...], { input: diffText })` piping the diff via stdin — same argv-array-only convention as every other Git integration in this product, never a shell string or a temp file. A failed apply is a normal, informative outcome (same convention as the Free Mode test runner reporting a failed test run), not an HTTP error: the route returns 200 with `status: "failed"` and the real `git apply` stderr in a new `apply_error` column (migration 007, a plain `ALTER TABLE ADD COLUMN`, verified safe against a simulated pre-existing database with real Phase 17 data). Findings page: the "Patches" panel gained "Approve diff for apply"/"Reject" buttons on a `proposed` patch (showing the diff itself right there for review), an "Apply patch" button once `approved_for_apply`, an "Applied to disk." success message, and a "Retry apply" button plus the real error text on `failed`. Deliberately still scoped: applying a patch does not automatically re-run tests, run AI self-review, or do anything else in docs/AI_MODE.md §4's later workflow steps ("Run Tests → AI Diagnosis...") — those remain Phase 19-21 territory; a user can manually re-run the existing Free Mode test runner after applying. 10 new backend tests (4 `applyPatch.test.ts` against real temp git repos incl. a real successful write, a real dry-run-catches-drift failure leaving the file untouched, and a malformed-diff clean failure; 4 new route tests incl. the full second-gate lifecycle with a real file changing on disk, reject-then-refuse-apply, and a failed-then-retried-successfully apply) — 245 backend tests total. 5 new frontend tests (approve-for-apply, reject, successful apply, failed apply with retry) — 66 frontend tests total. Dogfooded end-to-end against a real scratch COPY of this project's own `backend/src` (never the live source tree, since `/apply` really writes to disk): registered the copy, ran a real analysis (53 findings on the full backend/src at that scan depth), generated a real fix plan and a real diff for a genuine `large-function` finding, confirmed the second gate refused a premature `/apply`, then approved and applied — confirming the real target file on the scratch copy's disk was genuinely modified with the AI-generated content, not just a database row. |
| AI test generation | P1 | Done (tested) | Phase 19 — implements docs/AI_MODE.md §1's "AI-generated tests (reviewed & executed, not trusted on compile alone)". Mirrors Phases 17-18's two-gate `patch` state machine shape via new `generated_test`/`generated_test_review` tables (migration 008, purely additive — two fresh `CREATE TABLE`s, no rebuild): `pending_approval -> approved -> proposed` (has real `target_path` + `test_code`) `-> approved_for_write -> written` (couldn't actually run) `\| passed \| failed_tests`, or `-> rejected` at either gate. Unlike patch generation/apply, this only ever creates a NEW file — `/write-and-run` refuses to overwrite any path that already exists (`resolveWithinRoot` guards against path traversal in the AI-proposed path first), so there's no dry-run-against-drift case the way Phase 18 has. `generateTest()` (`backend/src/ai/workflows/generateTest.ts`) reuses `runFindingWorkflow()` unchanged and folds the finding's latest Phase 16 fix plan's "Required tests" section into the prompt as grounding when one exists (`usedFixPlan`, same hand-off pattern as Phases 17-18), asking the provider for two sections via the shared `parseStructuredSections()` parser — `TARGET_PATH:` and `TEST_CODE:` — or `NO_TEST: <reason>` if it can't produce one, preserved verbatim. A markdown code fence wrapping the whole `TEST_CODE` section (a common provider habit despite being told not to) is stripped as a light post-processing step. The defining difference from Phase 17's diffs: `/write-and-run` is the only route that both writes AND immediately executes what it wrote, via the real, existing Phase 9 test runner (`runTests()`) — the file is written first, then the project's actual detected test command runs, and a real `test_run` row is persisted and linked (`test_run_id`), independently fetchable via the existing Tests page API. A test command reporting exit code 0 marks the generated test `passed`; nonzero marks `failed_tests`; no supported command detected marks `written` (an honest degrade — the file did get created, it just couldn't be verified) rather than a failure. `written`/`failed_tests`/`passed` are all valid entry points for a retry via `/write-and-run` again (e.g. after fixing something elsewhere) without re-approving the same code. API: `POST/GET /api/v1/projects/:id/findings/:findingId/generated-tests`, `GET /api/v1/projects/:id/generated-tests/:testId`, `POST .../approve`, `POST .../reject`, `POST .../generate`, `POST .../approve-write`, `POST .../reject-write`, `POST .../write-and-run`. Findings page: a "Generated tests" toggle mirroring the "Patches" panel's shape — create/approve/reject, a "Generate test" button, a second approve/reject-for-write gate showing the generated code, then "Write and run"/"Re-run tests", with distinct messages for written/passed/failed outcomes and the test code rendered in a `<pre>` block. 15 new backend tests (7 `generateTest.test.ts` incl. `parseGeneratedTest` unit tests for the code-fence-stripping and NO_TEST cases, 5 `generatedTestRepo.test.ts`, 4 new route tests incl. the full real lifecycle with a real file written and a real `node -e` test command actually executed, an overwrite-refusal case, and a reject-then-refuse-write case) — 261 backend tests total. 6 new frontend tests — 72 frontend tests total. Dogfooded end-to-end against a real scratch COPY of this project's own `backend/` (node_modules symlinked, never copied, so the real `vitest` binary actually runs; never the live source tree, since this feature really writes a file and really executes a test command): registered the copy, ran a real analysis (9 findings), generated a real fix plan and real test code for a genuine `large-function` finding, confirmed the second gate refused a premature write, then approved and ran write-and-run — the real generated test file landed on disk in the scratch copy's `test/` directory and the real, full `vitest` suite (262 tests) genuinely executed and passed, exit code 0. |
| AI failure diagnosis | P1 | Done (tested) | Phase 20 — implements docs/AI_MODE.md §4's "(if failure) AI Diagnosis" workflow step, and the first AI-Mode feature whose target is a failed `TestRun` rather than a `Finding` — the `TestFailureTarget` kind docs/AI_MODE.md §3 named but deferred since Phase 13. Read-only, like root-cause analysis: never writes to the test run, never proposes an applied change (an actual fix still goes through the existing human-approval-gated fix-plan → patch-generation → diff-review flow). New `selectContextForTestFailure()` (`backend/src/ai/context/selectContextForTestFailure.ts`) builds a context bundle grounded entirely in the failed run's own real captured stdout/stderr: the output excerpt itself is always the primary item (truncated-to-fit the budget rather than ever excluded — the run's own output is the one thing this workflow can't work without), then real indexed files whose path text is actually found *inside* that output (ranked test-files-first, then by mention count — never a guess from the test command alone), then the top-matched failing test file's own imports (the modules it likely exercises). `diagnoseFailure()` (`backend/src/ai/workflows/diagnoseFailure.ts`) asks the provider for `LIKELY_CAUSE:`/`EVIDENCE:`/`SUGGESTED_DIRECTION:` via the same shared `parseStructuredSections()` parser root-cause analysis and fix planning use — deliberately no diff or code in the response, just a hypothesis, the evidence for it, and a pointer toward a fix direction. Migration 009 adds `ai_request.test_run_id` (nullable, purely additive — migration 005's original comment on `finding_id` explicitly anticipated this exact column), verified safe against a simulated pre-existing database with real accounting data already present. API: `POST /api/v1/projects/:id/tests/:runId/diagnose` (only callable when the run's `status` is `failed` — a passed, timed-out, or unsupported run has nothing to diagnose in this sense), `GET .../diagnosis` (read-only, reparses the stored raw response on every fetch, never calls a provider). Tests page: a "Diagnose failure" action appears only on a `failed` run, mirroring the Findings page's root-cause-analysis panel shape (evidence as bullets, likely cause and suggested direction as prose, raw response available in a collapsible details block), visibly disabled with an explanation when no AI provider is enabled. 20 new backend tests (7 `selectContextForTestFailure.test.ts`, 6 `diagnoseFailure.test.ts` incl. a real-HTTP-server provider-failure case that still persists an honest failed accounting row, 5 new route tests incl. a real failed `npm test` run genuinely executed and diagnosed end to end, 1 new `aiRequestRepo.test.ts` case proving `test_run_id`- and `finding_id`-keyed accounting rows stay independent) — 278 backend tests total. 4 new frontend tests — 76 frontend tests total. Dogfooded end-to-end against a real scratch COPY of this project's own `backend/` (node_modules symlinked, never the live source tree): registered the copy, deliberately broke a real function (`estimateTokens()`'s divisor) to produce a genuine test failure, ran the real, full `vitest` suite via the real test runner (278 tests, 2 real failures), diagnosed the real failure against a real local HTTP AI-provider double, and confirmed the context bundle actually selected the real captured output plus real files whose paths appeared in it — not synthetic fixtures. |
| AI self-review | P1 | Done (tested) | Phase 21 — implements docs/AI_MODE.md §6's self-review checklist (correctness, scope creep, regressions, security, missing tests, unnecessary complexity, consistency with existing architecture) run against a real proposed patch's real diff. Purely advisory, per §6's own wording ("shown alongside the diff, not used to auto-approve"): a self-review never changes the patch's `status` and is never a precondition for `/approve-apply` or `/apply` — it can be requested at any point once the patch has a real `diff_text`, including more than once for the same diff. Reuses `runFindingWorkflow` (self-review's grounding is the same Finding + context-bundle shape patch generation already uses — the patch adds only its diff text on top), unlike Phase 20's `diagnoseFailure`, which needed a genuinely different context shape for a `TestRun` target. `selfReviewPatch()` (`backend/src/ai/workflows/selfReview.ts`) asks for all seven checks in one response via the shared `parseStructuredSections()` parser, each expected as `STATUS: note` (`pass`/`concern`/`fail`); a check whose response doesn't match that shape keeps `status: null` but preserves whatever text was there as `note`, so nothing the model said is silently dropped even off-format. Migration 010 adds `ai_request.patch_id` (nullable, purely additive) — since a finding can have several patches and a patch can be regenerated more than once, scoping self-review's accounting by `finding_id` alone (like Phase 14) would blur together reviews of different diffs; `runFindingWorkflow` gained an optional `patchId` passthrough for this. Verified safe against a simulated pre-existing database with real accounting data already present. API: `POST /api/v1/projects/:id/patches/:patchId/self-review` (400 if the patch has no diff yet), `GET .../self-review` (read-only, reparses the stored raw response on every fetch, never calls a provider). Findings page: an "AI self-review" toggle inside each patch's card (visible only once it has a diff), showing a colored pass/concern/fail chip and one-sentence note per check, with the raw response available in a collapsible details block — visibly disabled with an explanation when no AI provider is enabled. 12 new backend tests (7 `selfReview.test.ts` incl. `parseSelfReviewSections` unit tests for case-insensitivity and off-format responses, 5 new route tests incl. a real generated patch self-reviewed end to end with its status verified untouched afterward), plus 1 new `aiRequestRepo.test.ts` case proving `patch_id`-keyed accounting rows for two different patches on the same finding stay independent — 291 backend tests total. 3 new frontend tests — 79 frontend tests total. Dogfooded end-to-end against a real scratch COPY of this project's own `backend/` (a genuinely introduced hardcoded-secret file, never the live source tree): registered the copy, ran a real analysis (a real `hardcoded-secret` finding), generated a real fix plan and a real diff against a real local AI-provider double, confirmed self-review correctly refuses a diff-less patch, then generated a real self-review of the real diff — correctness `pass`, missing tests `fail` — and confirmed the patch's own status stayed `proposed` throughout. |
| AI refactoring proposals | P2 | Not Started | Post-MVP |
| Autonomous multi-step agent execution | P3 | Explicitly out of scope | Not planned until foundations are stable |

## Platform / Scaffolding (Phase 0)

| Item | Status | Notes |
|---|---|---|
| Repository structure (`backend/`, `frontend/`, `docs/`, `progress/`) | Done (verified) | Created this session |
| Backend scaffold (Fastify + TS) builds & starts | Done (verified) | `npm run build`, health endpoint |
| SQLite connection + migration runner | Done (verified) | `schema_migrations` + `001_init.sql` |
| Frontend scaffold (React + Vite + TS + Tailwind) builds | Done (verified) | `npm run build` |
| Backend unit test executes (Vitest) | Done (verified) | 261 tests (smoke + discovery + indexing + nested gitignore + path sandboxing + architecture + analysis engine + git analysis + test runner + dependency analysis + security scan + audit report + AI provider adapter/registry/repo + AI context selection + AI explanation/root-cause/fix-plan/patch-generation/test-generation workflows + patch and generated-test state-machine repos + real-git-repo patch apply + API) |
| Frontend unit test executes (Vitest + RTL) | Done (verified) | 72 tests (nav shell, API client, Repositories/Dashboard/Architecture/Findings/Tests/Audit/AI Mode pages, incl. Dashboard's Git activity/Dependencies sections and Findings' AI explanation/root-cause/fix-plan/full patch lifecycle/full generated-test lifecycle UI) |
| Path sandboxing (`resolveWithinRoot`) | Done (tested) | Enforced at API layer for project registration; used by discovery |
| Repository registration UI ("Repositories" page) | Done (tested) | Register by absolute path, list, select, trigger discover+index ("Scan") — built alongside Phase 4 since Dashboard needs a project to display |
| Documentation set (`/docs/*.md`) | Done | This document set |
| Progress tracking (`/progress/*.md`) | Done | See progress/ |
| Deployment / self-hosting (Phase 25) | Done (tested) | See `docs/DEPLOYMENT.md` for the full guide, security caveats, and what's explicitly deferred. Scoped, like Phase 24, from `docs/PRD.md` §3/§7 — this product has never been a multi-tenant hosted service, so "self-hosting" means running it on a machine you control, not standing up a SaaS; documented plainly that there's no built-in authentication (every `/api/v1/*` route is open to anyone who can reach the port) and that a self-hoster exposing this beyond their own machine needs their own reverse proxy + auth, since building one wasn't decided here. Two paths shipped, both real and verified, not just written: (1) a multi-stage root `Dockerfile` (Debian-based, not Alpine, so `better-sqlite3`'s native addon built once in a `backend-build` stage stays glibc-compatible with the runtime stage it's copied into) that builds the frontend, then the backend (reusing Phase 24's existing `copy-frontend.mjs` unmodified — no Docker-specific build branch needed), installs `git` as a real runtime dependency (Git analysis/diff review/patch apply all shell out to it) in the final slim runtime image, and runs as the base image's existing unprivileged `node` user; a `docker-compose.yml` quick-start; both actually built and run end to end in this project's own dev sandbox — a real image was built, a real container started and logged serving the UI, real `curl` requests hit the health endpoint and served UI, a real host directory (this project's own `backend/src`) was bind-mounted in and registered as a project through the real API, and a real discover→index→analyze cycle produced 61 real findings inside the running container — then the container was destroyed and recreated against the same named volume, confirming the registered project and its data survived, not just the process staying up. (2) `deploy/codebase-engineer.service`, a systemd **user** unit (runs as the owning user, never root) for running the packaged Node process directly on a Linux host without Docker — verified with a real `systemd-analyze verify` pass (which caught a genuine portability bug in the first draft: a hardcoded `/usr/bin/node` path that doesn't exist in this dev sandbox's Node install, fixed to `env node` so it resolves via `PATH` instead) rather than a live-service run (no user D-Bus session available in this cloud dev container to actually start one) — documented honestly as verified-by-static-analysis, not verified-by-live-run, unlike the Docker path. No backend/frontend source changes this phase (pure deployment tooling + docs), so test counts are unchanged (300 backend / 79 frontend); both suites reverified stable regardless, since Dockerfile/systemd changes don't touch application code paths. **Repo-layout update (2026-08-19, post-Phase-26):** the Dockerfile and `docker-compose.yml` described above as living at the repo root were moved into `deploy/` alongside the systemd unit, at the user's explicit request, once `backend/`/`frontend`'s own separate git repos and the (previously nonexistent) root repo were being organized — the Docker build context is unchanged (still the repo root; only the Dockerfile/compose file's own location and the invocation flags moved), and `CHANGELOG.md` moved from the repo root into `docs/`. See `README.md`'s "Version control layout" section and `docs/DEPLOYMENT.md` for the current paths/commands. |
| Packaging — single-process combined app (Phase 24) | Done (tested) | See `docs/PACKAGING.md` for full scope, rationale, and what's deliberately deferred to Phase 25. Scoped from `docs/PRD.md` §3's "local-first" principle: no existing doc named a desktop-shell/installer approach, so Phase 24 was scoped to the concrete, low-risk step actually available — collapsing the dev-time split (separate Fastify API + separate Vite dev server on different ports, joined only by a dev proxy) into one process on one port a user can actually run, without changing the local-first architecture or making any unreviewed decision about Electron/Tauri/Docker/installers (explicitly deferred to Phase 25, "Deployment / self-hosting"). `buildApp()` (`backend/src/app.ts`) gained an optional `staticDir` — when set to a real built-frontend directory, it registers `@fastify/static` plus an SPA-fallback 404 handler (a direct load/refresh of a client-side route like `/findings` correctly re-serves `index.html`; an unmatched `/api/*` route still returns a real JSON 404, never silently masked by the SPA fallback, so a genuine "route doesn't exist" bug stays visible as an error instead of a confusing blank page). Omitted or invalid `staticDir` leaves the backend exactly as API-only as it's always been — purely additive, confirmed by the full pre-existing suite passing unmodified. `backend/scripts/copy-frontend.mjs` (plain Node `fs`, not a shell `cp -r` — same cross-platform reasoning as the existing `copy-migrations.mjs`) copies `frontend/dist` into `backend/dist/public` as part of `backend`'s own build, degrading safely (a log message, not a build failure) if the frontend hasn't been built yet. `config.ts`'s new `staticDir` resolution defaults to a location relative to the compiled module's own file (via `import.meta.url`, not `process.cwd()`) and is overridable via `CODEBASE_ENGINEER_STATIC_DIR`; only treated as real when `<dir>/index.html` actually exists, which correctly resolves to `null` during the normal `vitest` run. 8 new backend tests (`staticServing.test.ts`) build a real fixture "frontend build" on disk and issue real `app.inject()` requests through the real Fastify + `@fastify/static` pipeline, including a genuine path-traversal attempt confirming a file just outside the static root is never served — 300 backend tests total (was 292). Dogfooded fully end to end, not just via `app.inject()`: built the real frontend, ran the real backend build (which copied it in), started the real combined process (`node dist/server.js`) with a real temp data directory and a non-default port, and issued real `curl` requests confirming `GET /` served the real `index.html`, a real static asset under `/assets/` was served, `GET /findings` (client-side-only route) correctly fell back to `index.html` instead of 404ing, `GET /api/v1/health` still returned the real API response, and `GET /api/v1/<nonexistent>` returned a real JSON 404. Frontend unchanged this phase (still 79/79) — README updated with the single-process quick-start and a corrected, previously-stale "Phase 0/1" status line. |
| Performance optimization pass (Phase 23) | Done (tested) | Measured first, then fixed what the numbers actually showed — see `docs/PERFORMANCE.md` for full methodology and data. Profiled the real discover→index→analyze pipeline directly (not through HTTP) against a real 3,672-file, 131MB corpus (this environment's own Node.js toolchain `node_modules`, registered as a project root — used because it's large, real, and messy without needing network access to clone a public repo). Found `runAnalysis` dominated the ~3.6s full-cycle baseline (~2.5-2.7s of it), and within that, the `missing-test-file` rule alone cost ~1.4s — over half of `runAnalysis`'s time. Root cause: `missingTests.ts`'s `hasCorrespondingTest()` re-scanned every path in the repo, from scratch, for every candidate source file — an O(files × allPaths) algorithm. Fixed by precomputing the set of naming-convention-covered base names once in a single O(allPaths) pass (`collectBaseNamesWithCorrespondingTest()`), turning each candidate's check into an O(1) `Set` lookup — identical matching semantics, confirmed by the rule's finding count on the profiling corpus being unchanged (1,703) before and after, and all 5 pre-existing correctness tests passing unmodified. Result: the rule dropped ~30x (1,369ms → 46ms), `runAnalysis` dropped ~2.6x, and the full scan cycle dropped ~48% (3.6s → 1.9s) on the same corpus. Added a real regression-guard test (`backend/test/analysis.test.ts`) that builds a synthetic 1,500-file worst-case repo and asserts the rule stays under a 400ms bound the old O(N²) code would have blown through at that scale, catching any future reintroduction of the same class of bug. Two other repeated-full-tree-walk costs (discover/index/analysis each walking independently) and three other rules' per-file regex costs were profiled and found to be linear, not quadratic, or a deliberate freshness-over-speed design tradeoff already documented in `analysis/context.ts` — left unoptimized rather than "fixed" without a real problem to fix, per `docs/PERFORMANCE.md`'s "known remaining costs" section. 292 backend tests (was 291) — 1 new. Frontend unaffected (no frontend changes this phase), still 79/79. Both suites reverified stable across 2 repeated runs, both builds clean. |
| Optional monetization architecture — Razorpay (Phase 26) | Done (tested) | Built on the user's explicit "Yes, build it now" confirmation after being asked, since `docs/ROADMAP.md` had marked this phase "Not Started — explicitly deferred" and `docs/PRD.md` §7 lists billing/payments as an MVP non-goal. Entirely opt-in: with no `RAZORPAY_KEY_ID`/`RAZORPAY_KEY_SECRET`/`RAZORPAY_WEBHOOK_SECRET` set (the default), every route and check is a no-op — usage is unlimited and Free Mode/AI-Mode-with-a-user's-own-key are unaffected. Isolation principle from `docs/MONETIZATION.md` §3 honored: a self-contained `backend/src/billing/` module (`config.ts`, `razorpayClient.ts` — real `fetch`-based Orders API client tested against a real local HTTP double, `webhookVerify.ts` — real HMAC-SHA256 signature verification via `crypto.timingSafeEqual`, `subscriptionRepo.ts` — single-row-singleton subscription state since this product is single-instance/local-first per PRD §7, `usageLimiter.ts`) with its own additive migration (`011_billing.sql`: `subscription`, `billing_webhook_event` tables, migration-safety-verified against a simulated pre-existing database with real `project`/`ai_request` rows), never imported by `analysis/`, `ai/`, `git/`, or `patch/`, and reading — never writing — the existing billing-agnostic `ai_request` table. Enforcement is a single choke point: `routes/projects.ts`'s `resolveEnabledProvider()`, already the shared helper all 7 AI-spending routes call, now calls `checkAiOperationAllowed()` first and returns a 402 before resolving a provider once the free tier's monthly cap (50 AI operations) is reached — required zero changes to any of the 7 individual route handlers, since they already handle the generic `{error: {status, message}}` shape. `routes/billing.ts` adds `GET /api/v1/billing/status`, `POST /api/v1/billing/checkout` (real Razorpay order creation), and `POST /api/v1/billing/webhook` (raw-body parsing scoped to only this one route via its own encapsulated Fastify plugin context, so every other route keeps normal JSON parsing) — a one-time-order-plus-webhook-verified-payment flow granting a 30-day Pro period, explicitly not real Razorpay Subscriptions (recurring billing), documented as a natural next step rather than attempted speculatively. Frontend: `frontend/src/pages/Billing.tsx`, now served at `/settings` (replacing the placeholder), shows configured/unconfigured state, current tier, usage vs. limit with a visible near-limit/at-limit warning, and an "Upgrade to Pro" button; if the Razorpay Checkout widget script isn't loaded on the page it honestly reports the payment widget can't open (a real order is still created server-side) rather than fabricating success, consistent with this project's honest-degrade convention. 27 new backend tests (`razorpayClient`, `webhookVerify`, `subscriptionRepo`, `usageLimiter`, `routes.billing` — the last including a real end-to-end test making 50 real successful `/explain` calls then confirming a real 51st call is blocked with 402) bring the backend suite to 332/332; 7 new frontend tests bring the frontend suite to 86/86. Both suites reverified stable across 2 repeated runs, both builds (`tsc -b`/`vite build`) clean. |
| Security hardening pass (Phase 22) | Done (tested) | A systematic verification of `docs/SECURITY.md`'s controls against the real implementation, not new product surface — confirmed by code audit (not just re-reading the doc) that: (§2) every route reads a project's filesystem only via `project.root_path` (validated once, server-side, at registration by `assertValidProjectRoot`) — no route accepts an arbitrary client-supplied path for file reads, and the one AI-proposed-path write route (`generated-tests/:testId/write-and-run`) is the only place a request-influenced path reaches disk, and it's guarded by `resolveWithinRoot`; (§4) every content path that reaches an AI provider — `select.ts`'s Finding-target context bundle and `selectContextForTestFailure.ts`'s TestRun-target bundle, the only two context builders in the product — routes every file/diff/output string through `redactSecretsInText` before it's ever counted toward a token budget, with no bypass found; a generated patch's `diff_text` (passed raw into Phase 21 self-review's prompt) is safe by construction since it's the provider's own output, generated only from already-redacted context, never a re-read of the unredacted file; (§6) both write/execute routes (`patches/:patchId/apply`, `generated-tests/:testId/write-and-run`) check the persisted approval-gate status server-side before acting, not just in the UI, and the test runner only ever executes its own fixed, allow-listed command detection (`detectTestCommand()` — `mvn test` or the project's own `npm`/`pnpm`/`yarn` `test` script), never anything AI-supplied. Real, actionable finding from the dependency-review pass (§8): `npm audit` on `backend/` showed `fastify` (then pinned `^4.28.1`) had real high/critical-severity advisories only fixed by the `fastify` 5 major line, and `frontend/`'s `react-router-dom` (then `^6.26.1`) had a real moderate-severity open-redirect advisory only fixed by the `react-router-dom` 7 major line — both upgraded (`fastify@5.12.1`, `react-router-dom@7.18.2`) after confirming this product's actual usage of each (a handful of `app.get/post/patch/delete` + `request.body/params/query` + `reply.status` calls for Fastify; `BrowserRouter`/`Routes`/`Route`/`Link`/`NavLink`/`Outlet` for React Router, all stable, unchanged declarative-mode APIs across both major bumps) made the upgrade low-risk; full backend (291/291) and frontend (79/79) suites reverified passing and stable across 2 repeated runs on both, with clean rebuilds, confirming no regression. Remaining `npm audit` findings on both are exclusively in the `vite`/`vitest`/`esbuild` dev-tooling chain (dev-only, never shipped, and the underlying advisory only matters if a dev server is exposed to the network, which this project's test runs never do) — left as an accepted, documented risk rather than forcing a `vitest` major bump (`3.x -> 4.x`) across 34+9 test files for a low real-world-risk, dev-only issue; see `docs/SECURITY.md` §9. |

## Future / Monetization

| Feature | Priority | Status | Notes |
|---|---|---|---|
| Usage accounting (requests/tokens/limits) internal model | P1 | Not Started | Phase 12/13 groundwork; no billing |
| Razorpay billing integration | P3 | Done (tested) | See the Phase 26 row above and `/docs/MONETIZATION.md` |
| Team functionality | P3 | Not Started | Post-MVP |

**Rule:** nothing in this table is marked "Done" unless it has been implemented,
has real tests, the tests pass, the build passes, and it was manually verified.
Scaffold-level "Done (verified)" entries above refer only to the scaffold
functioning (builds/starts/tests execute) — not to any product feature being
implemented yet.
